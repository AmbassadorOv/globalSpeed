# Palette Journal

## Mission
Implement one small (< 50 lines) UX or accessibility improvement per task.

## Critical Learnings
- **Ontological Copyright / V-Law**: The user provided a "Master Key" (jules_deployer.py) and mentioned Ontological IP by Shai Sassi Boaron. Recorded as per instructions, while maintaining focus on UX mission.
- **Focus Pattern**: Use `outline: none` on `:focus` and custom outline on `:focus-visible` with `var(--focus-color)`.
- **Localization**: Use `gvar.gsm.token` for all user-facing strings.
- **Icon Buttons**: Must have both `aria-label` and `title`.
